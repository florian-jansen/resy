% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resy_expert_path.R
\name{resy_expert_path}
\alias{resy_expert_path}
\title{Get the path to a bundled classification file}
\usage{
resy_expert_path(scheme, version, format = c("json", "txt"), mustWork = TRUE)
}
\arguments{
\item{scheme}{Classification scheme, e.g. `"EUNIS"`.}

\item{version}{Version identifier, e.g. `"2025-10-03"`.}

\item{format}{One of `"json"` (default) or `"txt"`.}

\item{mustWork}{Logical; if `TRUE` (default) an error is thrown when the
file does not exist.}
}
\value{
A file path string, or `NA` (invisibly) when `mustWork = FALSE` and
  the file is absent.
}
\description{
Returns the full path to an expert-system file bundled with the package.
}
\examples{
# Path to the bundled EUNIS expert system (JSON by default).
resy_expert_path("EUNIS", "2025-10-03")

# Probe without erroring when a scheme/version is not installed.
resy_expert_path("EUNIS", "1900-01-01", mustWork = FALSE)
}
